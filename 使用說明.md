# 3D 頭骨拼圖

新版頭骨拼圖：介面、視覺效果與效能改版。桌面原始「頭骨」資料夾未變更。

## 環景背景

使用提供的古典教室圖片作為可隨視角轉動的環景，原圖存放於 assets/classroom-panorama.png。環景採用輕量背景球，略微調暗以維持骨骼辨識度，不改變骨骼的狀態顏色。圖片解析度為原始上傳版本，近距離觀看部分背景細節可能較柔和。

## 開啟

雙擊同資料夾的 **啟動拼圖.cmd**。需要 Python 3（目前這台電腦已安裝）。瀏覽器會自動開啟，啟動視窗請保持開啟，關閉視窗或按 Ctrl+C 即可停止。

請使用此啟動方式，不要直接雙擊 index.html；瀏覽器安全限制會阻擋本機檔案的模組與模型讀取。Three.js、BVH 程式與模型均已放在資料夾內，啟動後不需要連接 CDN。

## 操作

- 首頁先預覽組裝狀態。拖曳空白區旋轉，滾輪或雙指縮放。
- 按「開始拼圖」，將骨骼拖向中央透明輪廓。靠近目標會吸附，放開完成拼合；未到位則平滑返回。
- 點骨骼索引查看名稱與說明。「顯示這塊骨骼的位置」會短暫強調目標。
- 下方工具依序是回到預設視角、自動旋轉、透明目標與畫質。較慢裝置可選「流暢優先」。
- 13 塊全部就位後顯示完成時間，可欣賞模型或再次挑戰。
- 手機版上方骨骼索引可以左右滑動。
- 在支援 WebXR 的瀏覽器與裝置上會顯示 VR 入口；控制器扳機抓取／放開，搖桿移動。VR 模式扣下扳機可開始，完成後再扣可重開。遠端頭戴裝置通常需要 HTTPS；目前的 localhost 啟動器僅供本機使用。

## 頭骨姿態

已調整為直立姿態並校正左右傾斜，另已將下頷骨與下排牙齒整組移回上顎中線，並讓下頷骨連同下排牙齒上旋，呈現閉口展示效果。下頷後端已以前牙位置為固定支點上抬，向耳側靠攏。拼合目標已同步更新，預覽與組裝完成後使用同一姿態。

## 畫面文字精簡

已移除署名、裝飾標語、英文標籤與底部操作文字。操作方式仍可查閱本說明。

## 本次外觀調整

- 棕色背景、古典襯線字體與金色邊框。移除另取的名稱，標題只保留「3D 頭骨拼圖」。
- 預設使用側前方視角，首頁與完成後回到預設視角皆一致。
- 未拼合：象牙色；點選：琥珀金；已拼合：青綠色。重開時恢復象牙色。
- 拼圖片散開位置配合新視角調整，已重新測試 13 塊拖曳完成。

## 改版內容

- 棕底金邊古典介面、骨骼索引、進度計時、說明卡、提示與完成畫面。
- 柔和骨質材質、冷暖補光、展示底座、選取高亮、歸位動畫與輕量脈衝效果。
- 以提供的 `20250908.glb` 還原各骨骼的相對位置，取代舊版個別置中後手填的位置。沿用 13 塊可操作拼圖片；下排牙齒與下頷骨一起移動，其餘輔助骨骼作固定背景結構。
- 模型預先轉成索引幾何，並進行有誤差限制、考慮法線的簡化：完整資產由 1,126,157 降至 796,154 個三角形，減少約 29.3%。這是視覺模型的簡化，不是無損處理；原始模型仍保留在桌面資料夾。
- 每塊模型只讀取一次，透明目標與拼圖片共用幾何。Worker 在背景建構 BVH，選取不再逐一掃描所有三角形。
- 移除全解析度鏡面；限制像素倍率；靜止時停止重複繪圖；VR 面板僅在文字變化時更新紋理。

## 驗證與限制

- 已用這台電腦的無頭 Chrome 實測載入、全部 13 塊的滑鼠拖曳吸附、完成彈窗、重開、畫質切換與 390 × 844 手機排版。無 JavaScript 執行錯誤。
- 相同新版幾何的 130 條選取射線測試：兩次本機測試中，BVH 約 1.1–6.7 ms，標準逐三角形選取約 322–368 ms；最近命中的物件與距離結果一致。這是本機微型測試，並非整體 FPS 倍率，也不是跨裝置保證。
- 尚未以實體 VR 頭戴裝置測試，也尚未做解剖學專業審校。骨骼形狀來自使用者提供的 GLB；整體直立角度與下頷閉合姿態已依展示需求調整。
- `model-stats.json` 記錄逐骨骼的三角形、檔案大小與簡化誤差。瀏覽器主控台可呼叫 `skullDiagnostics()` 查看渲染資料。

## 原始碼與授權

介面：`index.html`、`style.css`；互動：`app.js`；背景模型處理：`model-worker.js`；定義：`bones.json`、`context.json`。

Three.js 0.160.0、three-mesh-bvh 0.7.6 的授權文件在 `vendor/`。預處理使用 meshoptimizer 0.20.0，授權文件同樣附上。模型來自使用者提供的資料，不另行主張模型授權。

BVH 實作參考：[three-mesh-bvh 官方專案](https://github.com/gkjohnson/three-mesh-bvh)。
